# Custom Generative Video Engine

ระบบสร้างวิดีโออัตโนมัติระดับ Pixel และ Audio Frequency $100\%$ โดยไม่พึ่งพาไฟล์มีเดียสำเร็จรูปและไม่ใช้ไลบรารี AI ของผู้อื่น

## การติดตั้งและใช้งาน
1. รัน Python Backend เพื่อดึงและประมวลผลข้อมูล:
   ```bash
   pip install -r requirements.txt
   python backend/scraper.py
   python backend/analyzer.py
